steps:
  - label: ":terraform: Initialize"
    command: "../scripts/init_terraform.sh"

  - wait

  - label: ":clipboard: Plan"
    command: "../scripts/plan_terraform.sh"

  - wait

  - block: "Apply"
    prompt: "Apply these changes?"

  - label: ":rocket: Apply"
    command: "../scripts/apply_terraform.sh"
